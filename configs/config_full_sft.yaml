defaults:
  - model: llama3_8b
  - dataset: chatdoctor_sample
  - optimization: default
  - sequence: default
  - checkpointing: default
  - runtime: gpu
  - _self_

run:
  phase: P1
  domain: medical
  fma_method: full_sft
  seed: 42
  run_id: ???
  experiment_name: ???
