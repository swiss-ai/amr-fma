name: iamtarun/python_code_instructions_18k_alpaca
split: train
text_field: output  # _format_example handles instruction+input+output schema
max_samples: null  # use the full ~18k samples for a real fine-tuning run
eval_samples: 512
